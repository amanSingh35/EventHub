{
  "version": 2,
  "builds": [
    {
      "src": "vite.config.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "scripts": {
    "postinstall": "npm install vite && chmod +x node_modules/.bin/*",
    "build": "vite build"
  }
}
